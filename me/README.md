# Profile data for the chat agent

Place your files here:

- **`summary.txt`** – Short bio used by the agent (plain text).
- **`resume.pdf`** – Export of your LinkedIn profile (PDF). The app reads it for context; the file is in `.gitignore` so it won’t be committed.

You can copy them from `1_foundations/me/` in the original agents repo.
